/* import { getDocContent, getPosts, onSnapshot, collection, addPost } from "../Firebase/firestore.js";
import { db } from "../Firebase/firebaseConfig.js";
import { auth, viewer } from "../Firebase/firebase.js"; 
import { feed } from "../components/feed.js";
 

 export const postPrint = (doc) => {
    
   let postValue = document.createElement('div')
    postValue.innerHTML += `
    <div id = "newPostContainer" class= "newPostContainer"> sí funciona gracias a dios ${doc.post} </div>
    `
    console.log("doc desde post.js")
    
    return postValue
 }

 */

